# Local PDF inputs

Place PDFs that you are authorized to process in this directory before running
the optional PDF extraction workflow. PDF inputs and their derived parquet are
ignored by Git and excluded from the Docker build context.

The maintained public repository does not redistribute the historical book
chapters used during capstone exploration. The runtime's versioned Wikipedia
artifacts are documented in the root README and `docs/REPRODUCIBILITY.md`.
